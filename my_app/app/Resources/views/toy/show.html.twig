{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{toy.name }}</h1>

    <table>
        <tbody>
            
            <tr>
                <th>Radość</th>
                <td>{{ toy.hapiness }}</td>
            </tr>
            <tr>
                <th>Inteligencja</th>
                <td>{{ toy.intelligence }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('toy_index') }}">Powrót</a>
        </li>
        <li>
            <a href="{{ path('toy_edit', { 'id': toy.id }) }}">Edytuj</a>
        </li>
    </ul>
       
            {{ form_start(delete_form) }}
                <input type="submit" value="Usuń">
            {{ form_end(delete_form) }}
        
{% endblock %}
